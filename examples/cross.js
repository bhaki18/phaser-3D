class Example extends Phaser.Scene {     create() { this.add.text(10, 10, 'Vector3.cross(v)\nRed x Green = Blue (Orthogonal)', {fill:'#0f0'});         this.camera = new Camera(); this.camera.position = new Vector3(0,0,-40);         this.renderer = new Render(this, this.camera, {renderScale:1});         this.c1 = Mesh.createCube(1); this.c1.scale = new Vector3(1, 10, 1); this.c1.baseColor = 0xff0000;         this.c2 = Mesh.createCube(1); this.c2.scale = new Vector3(10, 1, 1); this.c2.baseColor = 0x00ff00;         this.c3 = Mesh.createCube(1); this.c3.scale = new Vector3(1, 1, 10); this.c3.baseColor = 0x0000ff;         this.meshes = [this.c1, this.c2, this.c3];     }     update(t, d) {         this.renderer.clearBuffers();         let angle = t * 0.001;         this.c1.rotation.z = angle;         this.renderer.render(this.meshes);     } } const game = new Phaser.Game({type: Phaser.AUTO, width: 800, height: 600, scene: Example});